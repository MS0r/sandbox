services:
  app-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: erlang-box-dev
    volumes:
      - .:/workspace:cached
    ports:
      - "5555:4000"
    command: /bin/sh -c "while sleep 1000; do :; done"
    tty: true